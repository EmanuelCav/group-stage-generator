import { StyleSheet } from "react-native";

export const groupStyles = StyleSheet.create({

    rowContainer: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1
    }

})